import { styles } from "@components/common/SellerChip/styles.js";

export const SellerChip = () => {
    return (
        <div style={ styles.sellerChip }>
            My selling product
        </div>
    );
}